---
title: 蛊真人
published: 2026-06-28
image: ./cover.png
tags: [蛊真人, 新角色]
category: 新角色
draft: false
pinned: true
---

## 下载资源

<div class="prereq-notice">
  <span class="prereq-label">⚠️ 需要前置模组：<strong>BaseLib.3.3.2</strong></span>
</div>

<div class="download-list">

<div class="download-item">
  <span class="download-label">📥 前置文件 <small>自动解析 BaseLib 最新版</small></span>
  <a class="no-styling" href="javascript:void(0)" onclick="startRedirect()">下载</a>
</div>

<div class="download-item">
  <span class="download-label">📦 本体文件</span>
  <a class="no-styling" href="https://pan.quark.cn/s/4223d651f627" target="_blank">获取</a>
</div>

</div>

<!-- 跳转毛玻璃遮罩 -->
<div id="redirect-overlay" class="redirect-overlay-hidden">
  <div class="redirect-card">
    <div class="redirect-spinner">
      <div class="spinner-ring"></div>
    </div>
    <p class="redirect-text">正在跳转</p>
    <p class="redirect-sub">自动定位 BaseLib 最新版本下载页…</p>
  </div>
</div>

<style>
.prereq-notice {
  display: flex;
  align-items: center;
  gap: .5rem;
  padding: .6rem .85rem;
  border-radius: .5rem;
  color: #e67e22;
  background: rgba(230,126,34,.08);
  border: 1px solid rgba(230,126,34,.2);
  font-size: .85rem;
  margin-bottom: .75rem;
  width: fit-content;
}
.prereq-notice strong {
  font-weight: 700;
  color: inherit;
  font-size: inherit;
}
.dark .prereq-notice {
  color: #f0a04b;
  background: rgba(240,160,75,.1);
  border-color: rgba(240,160,75,.2);
}
.redirect-overlay-hidden {
  display: none;
}
#redirect-overlay {
  position: fixed;
  inset: 0;
  z-index: 99999;
  background: rgba(0, 0, 0, 0.45);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  align-items: center;
  justify-content: center;
  flex-direction: column;
  cursor: default;
  pointer-events: all;
}
#redirect-overlay.active {
  display: flex;
}
.redirect-card {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.75rem;
  padding: 2rem 2.5rem;
  border-radius: 1rem;
  background: rgba(255, 255, 255, 0.08);
  border: 1px solid rgba(255, 255, 255, 0.12);
}
.redirect-spinner {
  width: 48px;
  height: 48px;
  display: flex;
  align-items: center;
  justify-content: center;
}
.spinner-ring {
  width: 36px;
  height: 36px;
  border: 3px solid rgba(255, 255, 255, 0.2);
  border-top-color: #fff;
  border-radius: 50%;
  animation: redirect-spin 0.8s linear infinite;
}
@keyframes redirect-spin {
  to { transform: rotate(360deg); }
}
.redirect-text {
  color: #fff;
  font-size: 1.2rem;
  font-weight: 600;
  margin: 0;
  letter-spacing: 0.05em;
}
.redirect-sub {
  color: rgba(255, 255, 255, 0.6);
  font-size: 0.85rem;
  margin: 0;
}


</style>

<script>
  // 页面加载时清理残留遮罩，确保初始状态正确
	;(function resetOverlay() {
	  var leftover = document.querySelector('body > #redirect-overlay');
	  if (leftover) leftover.remove();
	  var overlay = document.getElementById('redirect-overlay');
	  if (overlay) overlay.classList.remove('active');
	})();
	document.addEventListener('astro:page-load', resetOverlay);
	document.addEventListener('swup:contentReplaced', resetOverlay);

function startRedirect() {
  var overlay = document.getElementById('redirect-overlay');
  // 移到 body 下避免 #main-grid transform 导致 fixed 定位失效
  document.body.appendChild(overlay);
  overlay.classList.add('active');
  // 禁止页面滚动
  document.documentElement.style.overflow = 'hidden';
  document.body.style.overflow = 'hidden';
  // Swup SPA 跳转到 releases 页面，避免整页刷新闪烁
  setTimeout(function() {
    // 恢复滚动，避免带到下个页面
    document.documentElement.style.overflow = '';
    document.body.style.overflow = '';
    var url = '/releases/?repo=Alchyr%2FBaseLib-StS2&version=latest&open=1';
    if (window.swup) {
      window.swup.navigate(url);
    } else {
      window.location.href = url;
    }
  }, 1200);
}
</script>

> [!NOTE]
> 📂 该模组位于网盘路径 `角色MOD/蛊真人` 下。